<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppThemeWithNoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/action_bar</item>
        <item name="colorPrimaryDark">@color/dark_action_bar</item>
        <item name="android:textColor">@color/brown</item>
        <item name="android:navigationBarColor">@color/navigation_dark_action_bar</item>
        <item name="android:textSize">18sp</item>
    </style>

    <!-- Login  -->

    <style name="LoginTheme">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="BrownTheme" parent="Theme.AppCompat.Light.DarkActionBar" >
        <item name="colorPrimary">@color/action_bar</item>
        <item name="colorPrimaryDark">@color/dark_action_bar</item>
        <item name="android:textColor">@color/brown</item>
        <item name="android:navigationBarColor">@color/navigation_dark_action_bar</item>
        <item name="android:textSize">18sp</item>
    </style>


    <!-- ...............................................  -->

    <!--Main Shopping list  -->

    <style name="FAB">
        <item name="fabSize">normal</item>
        <item name="rippleColor">@android:color/white</item>
        <item name="backgroundTint">@color/light_blue</item>
    </style>

    <!-- ...............................................  -->

    <!-- OCR -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />



</resources>
